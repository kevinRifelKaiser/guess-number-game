import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#9DAABE',
    height: 120,
    justifyContent: 'center',
    alignItems: "center",
    paddingTop: 20
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold'
  }
});